Slides guidance

Place slide decks in `slides/` within an event repo. Avoid storing large binary files in core. Include speaker notes and a short changelog.

Naming suggestion: `YYYY-MM-DD-eventname-starter-deck.pptx` or `eventname-starter-deck.pptx`.
